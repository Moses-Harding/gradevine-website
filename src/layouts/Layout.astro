---
interface Props {
  title: string;
  description: string;
  canonical?: string;
}

const { title, description, canonical } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#5002F7" />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://gradevine.app" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />

    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="icon" type="image/png" sizes="1024x1024" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta property="og:image" content="https://gradevine.app/og-image.png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:image" content="https://gradevine.app/og-image.png" />
    <title>{title}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link rel="canonical" href={canonical || "https://gradevine.app/"} />
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "GradeVine",
      "operatingSystem": "iOS",
      "applicationCategory": "EducationApplication",
      "offers": [
        { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
        { "@type": "Offer", "price": "4.99", "priceCurrency": "USD" },
        { "@type": "Offer", "price": "9.99", "priceCurrency": "USD" }
      ],
      "url": "https://apps.apple.com/app/id6755069483"
    })} />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  :root {
    /* Brand — matches iOS app Colors.swift */
    --color-purple-deep: #5002F7;
    --color-purple: #7002F7;
    --color-purple-light: #f0e6ff;
    --color-purple-50: #f5f0ff;

    --color-red: #FF1C36;
    --color-coral: #FF6B6B;

    --color-yellow: #FFD24C;
    --color-yellow-light: #FFE385;

    --color-blue: #3880F5;
    --color-blue-light: rgba(56, 128, 245, 0.08);

    /* Neutral */
    --color-gray-50: #fafafa;
    --color-gray-100: #f5f5f5;
    --color-gray-200: #eeeeee;
    --color-gray-300: #e0e0e0;
    --color-gray-500: #9e9e9e;
    --color-gray-600: #757575;
    --color-gray-700: #616161;
    --color-gray-800: #424242;
    --color-gray-900: #212121;

    /* Shadows — matches iOS shadowColor values */
    --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.08);
    --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.10);
    --shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.12);

    /* Radii — matches iOS CornerRadius values */
    --radius-sm: 10px;
    --radius-button: 12px;
    --radius-card: 16px;
    --radius-lg: 24px;
    --radius-pill: 999px;

    --font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI',
      sans-serif;
    --max-width: 1120px;
  }

  html {
    font-family: var(--font-family);
    color: var(--color-gray-900);
    scroll-behavior: smooth;
    -webkit-font-smoothing: antialiased;
  }

  body {
    min-height: 100vh;
    background: #fff;
  }

  img {
    max-width: 100%;
    height: auto;
    display: block;
  }

  a {
    color: var(--color-purple-deep);
    text-decoration: none;
  }

  a:hover {
    text-decoration: underline;
  }
</style>
